# docs

design docs specific to the haruspex crate/package:

- [automerge-spike.md](automerge-spike.md) - the automerge-free-core design spike (settled:
  core stays automerge-free; an adapter subpath is added only if a concrete resource needs
  genuine multi-writer merge)

the main [README](../README.md) has architecture, structure, and getting-started examples.
the canonical refactor plan (phases, decision log, open questions) lives in
tomb/docs/xl-refactor/.
