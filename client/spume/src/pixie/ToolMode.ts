// two primary modes for the canvas editor
export type ToolMode = "navigate" | "edit";

// what to create when the add button flyout is used
export type AddAction = "grid" | "shelf" | "bin" | "label";
