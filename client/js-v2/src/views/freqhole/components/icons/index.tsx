// Legacy icon imports - redirects to new UI icons location
// This file maintains backward compatibility while we transition to the new icon system

export * from "../ui/icons/index.js";
export * from "../ui/icons/registry.js";

// For any components still using the old import path
// TODO: Update import paths to use ../ui/icons/ directly
