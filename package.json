{
    "name": "freqhole-release",
    "version": "0.1.29",
    "private": true,
    "description": "release coordination (changesets + version sync) for freqhole. not published.",
    "type": "module",
    "scripts": {
        "changeset": "changeset",
        "changeset:status": "changeset status",
        "version": "changeset version && make bump-version NEW_VERSION=$(node -p \"require('./package.json').version\") && npm --prefix freqhole.net run gen:dates",
        "release:publish": "node scripts/release-publish.mjs"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.10"
    },
    "overrides": {
        "@types/node": "^12.7.1"
    }
}