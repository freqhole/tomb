// phase 0 skeleton - filled in during phase 5 (./solid: auth forms, knock inbox, add-remote
// flow FSM, qr modal/scanner, federation settings panel). solid-js is an optional peer dep -
// keep this subpath the only one that imports it.
export {};
